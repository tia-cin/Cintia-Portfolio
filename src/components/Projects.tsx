import React from "react";

const Projects: React.FC<{
  projects: any[];
}> = ({ projects }) => {
  return (
    <div className="flex flex-col justify-center items-center relative w-full min-h-screen overflow-hidden"></div>
  );
};

export default Projects;
